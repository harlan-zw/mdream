{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.11.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/harlan-zw/mdream"
  },
  "scripts": {
    "bundle-size:sync": "pnpm test:bundle-size && node bench/bundle/analyze.ts && node bench/bundle/update.ts",
    "build": "pnpm run -r build",
    "lint": "eslint .",
    "typecheck": "pnpm run -r typecheck",
    "test": "pnpm exec playwright install && vitest test",
    "test:bundle-size": "pnpm --filter @mdream/js build && obuild --dir bench/bundle",
    "release": "pnpm build && bumpp --all --output=CHANGELOG.md --execute=\"node scripts/sync-cargo-version.mjs\" packages/*/package.json",
    "release:beta": "pnpm build && bumpp --all --release prerelease --preid beta --output=CHANGELOG.md --execute=\"node scripts/sync-cargo-version.mjs\" packages/*/package.json",
    "test:attw": "pnpm -r --parallel --filter './packages/**' run test:attw",
    "bench": "vitest bench --config bench/vitest.config.ts",
    "bench:cli": "./bench/cli-compare.sh",
    "knip": "knip"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@arethetypeswrong/cli": "catalog:",
    "@kreuzberg/html-to-markdown-node": "catalog:bench",
    "@mdream/js": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "bumpp": "catalog:",
    "eslint": "catalog:",
    "knip": "catalog:",
    "node-html-markdown": "catalog:bench",
    "obuild": "catalog:",
    "pathe": "catalog:",
    "playwright": "catalog:",
    "rehype-parse": "catalog:bench",
    "rehype-remark": "catalog:bench",
    "remark-gfm": "catalog:bench",
    "remark-stringify": "catalog:bench",
    "turndown": "catalog:bench",
    "turndown-plugin-gfm": "catalog:bench",
    "typescript": "catalog:",
    "unified": "catalog:bench",
    "vitest": "catalog:"
  }
}
