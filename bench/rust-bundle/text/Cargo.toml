[package]
name = "text"

version.workspace = true

edition.workspace = true

rust-version.workspace = true

publish.workspace = true

[dependencies]
mdream-rust-bundle = { path = "../common" }
mdream = { path = "../../../crates/core", default-features = false, features = [ "text" ] }
